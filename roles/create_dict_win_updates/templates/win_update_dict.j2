{% for win_update in list_of_updates.updates | dict2items %}
   {
     "update": {
       "kb": [
         "{{ win_update.kb }}"
       ],
       "hostname: "{{inventory_hostname }}",
       "installed": false,
       "id": "{{ win_update.id }}",
       "title": "{{ win_update.title }}"
     }
   }{{ "," if not loop.last }}
{% endfor %}   