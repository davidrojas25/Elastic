{% for win_update in list_of_updates.updates | dict2items %}
   {
     "update": {
       "kb": [
         "{{ win_update.updates.kb }}"
       ],
       "hostname: "{{inventory_hostname }}",
       "installed": false,
       "id": "{{ win_update.updates.id }}",
       "title": "{{ win_update.updates.title }}"
     }
   }{{ "," if not loop.last }}
{% endfor %}   